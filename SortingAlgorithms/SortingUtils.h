#ifndef SORTING_UTILS_H
#define SORTING_UTILS_H

/**
 * Swaps the values in the memory addresses of given parameters
 */
void swap(int *a, int *b);

/**
 * Prints the given array on the console
 */
void printArray(int *array);

#endif